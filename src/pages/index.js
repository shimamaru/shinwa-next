// import Head from "next/head";
// import Layout from "../components/Layout";
// import { getAllPosts } from "../../lib/notionAPI";

// export const getStaticProps = async () => {
//   const allPost = await getAllPosts();

//   return {
//     props: {
//       allPost,
//     },

//     revalidate: 60,
//   };
// };

// export default function Home() {
//   console.log("allPost", allPost);
//   return (
//     <>
//       <Head>
//         <title>Create Next App</title>
//         <meta name="description" content="Generated by create next app" />
//         <meta name="viewport" content="width=device-width, initial-scale=1" />
//         <link rel="icon" href="/favicon.ico" />
//       </Head>
//       <main>
//         <Layout>
//           <div className="flex items-center justify-center w-full mb-10 "></div>
//         </Layout>
//       </main>
//     </>
//   );
// }

export async function getServerSideProps() {
  const res = await fetch("http://localhost:3000/api/news");
  const news = await res.json();
  console.log("news data:", news); // コンソールにデータを出力

  return {
    props: {
      news,
    },
  };
}

export default function NewsPage({ news }) {
  return (
    <div>
      <h1>お知らせ</h1>
      <ul>
        {news.map((item) => (
          <li key={item.id}>{item}</li>
        ))}
      </ul>
      <p>{news}</p>
    </div>
  );
}
// import { Client } from "@notionhq/client";

// export default function YourPageComponent({ data }) {
//   // dataを使用してページをレンダリングするなどの処理を行う
//   <div>
//     <h1>お知らせ</h1>
//     <ul>
//       {data.map((item) => (
//         <li key={item.id}>{item}
//           <p>{page}</p>
//         </li>
//       ))}
//     </ul>
//     <p>{data}</p>
//   </div>;
// }

// export async function getStaticProps() {
//   const notion = new Client({ auth: process.env.NOTION_API_KEY });
//   const databaseId = process.env.NOTION_DATABASE_ID;
//   const response = await notion.databases.query({
//     database_id: databaseId,
//   });

//   console.log(response); // このログが出力されるはずです

//   return {
//     props: {
//       data: response,
//     },
//   };
// }
